(function(ns) {
    'use strict';

    window.tracker = ns = (ns || {});

    // ns.getRepo = function getRepo(u) {
    //     $.ajax({
    //         url: 'https://api.github.com/repos' + ns.username +
    //     })
    // }


})(window.tracker);
